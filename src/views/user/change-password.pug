extends ../base
block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(type="password",placeholder="old password",name="oldPassword",required)
        input(type="password",placeholder="new password",name="newPassoword",required)
        input(type="password",placeholder="Confirm new Password",name="confirmNewPassowrd",required)
        input(type="submit",value="changePassword")