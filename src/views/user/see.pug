extends ../base
include ../mixin/video
block content
    section(class="user")
        div(class="user_info")
            img(src="/"+user.avatarUrl,class="user_avatar",style="width:100px")
            h1=user.username
    each item in user.videos
        +video(item)